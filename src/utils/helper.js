// export function filterData(searchText, results) {
//   const filterData = request.filter((results) =>
//     results?.data?.results?.toLowerCase()?.includes(searchText.toLowerCase())
//   );

//   return filterData;
// }
